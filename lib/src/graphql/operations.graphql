query RecipesByCursor($cursor: Cursor) {
  recipes(before: $cursor) {
    nodes {
      contributorId
      description
      id
      ingredients
      minutes
      name
      numberOfIngredients
      numberOfSteps
      nutrition
      steps
      submitted
      tags
    }
  }
}
